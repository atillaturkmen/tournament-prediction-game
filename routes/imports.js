// This file contains imported dependencies
// They can be accessed from this file when needed

const express = require("express");
const path = require("path");

module.exports = {
    express: express,
    path: path,
};